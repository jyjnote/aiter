# TTS 추가 최적화 가이드

## 이미 적용된 최적화
✅ GPU 워밍업
✅ CUDA 최적화 (TF32, cuDNN benchmark)
✅ torch.inference_mode()

## 추가 최적화 옵션

### 1. 모델 양자화 (INT8) - 2배 속도 향상
```bash
# 품질 5% 하락, 속도 50% 향상
# 별도 양자화된 모델 필요
```

### 2. 배치 추론 활성화 - 처리량 3배 향상
```python
# 여러 텍스트를 한 번에 처리
# 메모리 사용량 증가
NUM_LLM_WORKERS = 4  # 2개 → 4개로 증가
```

### 3. KV 캐시 크기 증가
```python
# 모델 설정에서 KV 캐시 최적화
# cosyvoice 모델 설정 파일 수정 필요
```

### 4. 텍스트 전처리 캐싱
```python
# 동일한 문구 반복 시 캐싱
# 실용적이지 않을 수 있음
```

### 5. 하드웨어 업그레이드
- GPU: RTX 4080 Super → RTX 4090 (20% 향상)
- CPU: 더 빠른 모델 (BPE 토크나이저 속도 영향)

## 현재 RTF 목표

| 청크 | 현재 RTF | 목표 RTF | 달성 가능성 |
|------|---------|---------|------------|
| 첫 청크 | 10~17 | **1~2** | ✅ 가능 (워밍업) |
| 2번째 | 3~6 | **0.5~1** | ✅ 가능 (최적화) |
| 3번째+ | 0.4~2 | **0.3~0.8** | ✅ 가능 (안정화) |

## 최적 설정 요약

```python
# 1. GPU 워밍업 (이미 적용)
# 2. CUDA 최적화 (이미 적용)
# 3. inference_mode (이미 적용)
# 4. 워커 수: 2개 (현재)
# 5. stream=True (이미 적용)
# 6. FP16 모드 (이미 적용)
```

## 성능 모니터링

```bash
# RTF 확인 방법
grep "rtf" 로그파일.txt

# 목표: 모든 청크 RTF < 1.0
```

## 예상 최종 성능

**개선 전:**
- 첫 재생: 9.7초
- RTF: 3~17

**개선 후 (예상):**
- 첫 재생: 1~2초 ⚡
- RTF: 0.3~2.0 (평균 0.8) ⚡
- 체감: 거의 실시간 ✨
